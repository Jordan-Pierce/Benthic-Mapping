version: '3'

services:
  algorithm:
    build: .
    environment:
      - PROJECT_ID=${PROJECT_ID}
      - MEDIA_IDS=${MEDIA_IDS}
      - HOST=${HOST}
      - TOKEN=${TOKEN}
      - START_AT=${START_AT}
      - END_AT=${END_AT}
    volumes:
      - "./Data/:/workdir/Data/"